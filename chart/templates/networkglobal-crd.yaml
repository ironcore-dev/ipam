apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: {{ .Values.crd.ng.plural }}.{{ .Values.crd.ng.group }}
spec:
  group: {{ .Values.crd.ng.group }}
  names:
    kind: {{ .Values.crd.ng.kind }}
    listKind: {{ .Values.crd.ng.listKind }}
    plural: {{ .Values.crd.ng.plural }}
    singular: {{ .Values.crd.ng.singular }}
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: Total address capacity in all ranges
          jsonPath: .status.capacity
          name: Capacity
          type: string
        - description: Description
          jsonPath: .spec.description
          name: Description
          type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: NetworkGlobal is the Schema for the networkglobals API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: NetworkGlobalSpec defines the desired state of NetworkGlobal
              properties:
                description:
                  description: Description contains a human readable description of
                    network
                  type: string
              type: object
            status:
              description: NetworkGlobalStatus defines the observed state of NetworkGlobal
              properties:
                capacity:
                  anyOf:
                    - type: integer
                    - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                ranges:
                  items:
                    type: string
                  type: array
              required:
                - ranges
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
