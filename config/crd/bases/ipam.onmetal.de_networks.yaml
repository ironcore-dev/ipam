
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: networks.ipam.onmetal.de
spec:
  group: ipam.onmetal.de
  names:
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Network Type
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Network ID
      jsonPath: .spec.id
      name: ID
      type: string
    - description: Total address capacity in all ranges
      jsonPath: .status.capacity
      name: Capacity
      type: string
    - description: Description
      jsonPath: .spec.description
      name: Description
      type: string
    - description: Request state
      jsonPath: .status.state
      name: State
      type: string
    - description: Message about request processing resutls
      jsonPath: .status.message
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Network is the Schema for the networks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSpec defines the desired state of Network
            properties:
              description:
                description: Description contains a human readable description of
                  network
                type: string
              id:
                description: ID is a unique network identifier. For VXLAN it is a
                  single 24 bit value. First 100 values are reserved. For MLPS it
                  is a set of 20 bit values. First 16 values are reserved. Represented
                  with number encoded to string.
                type: string
              type:
                description: NetworkType is a type of network id is assigned to.
                enum:
                - VXLAN
                - MPLS
                type: string
            type: object
          status:
            description: NetworkStatus defines the observed state of Network
            properties:
              capacity:
                anyOf:
                - type: integer
                - type: string
                description: Capacity is a total address capacity of all CIDRs in
                  Ranges
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              message:
                description: Message contains error details if the one has occurred
                type: string
              ranges:
                description: Ranges is a list of ranges booked by child subnets
                items:
                  type: string
                type: array
              state:
                description: State is a network creation request processing state
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
